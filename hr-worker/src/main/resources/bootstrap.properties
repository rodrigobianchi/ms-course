spring.application.name=hr-worker
spring.cloud.config.uri=http://localhost:8888

#atributo que define qual o profile ativo e recuperado através do server config
spring.profiles.active=test

#atributo da bliblioteca actuator que permite atualizar properties em runtime
management.endpoints.web.exposure.include=*
